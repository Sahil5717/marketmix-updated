import React from 'react';
import ReactDOM from 'react-dom/client';
import LoginApp from './client/LoginApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoginApp />
  </React.StrictMode>
);
