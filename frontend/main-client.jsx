import React from 'react';
import ReactDOM from 'react-dom/client';
import DiagnosisApp from './client/DiagnosisApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DiagnosisApp />
  </React.StrictMode>
);
